

<% if @trains.any? %>
  <ul>
    <% @trains.each do |train| %>
      <li><a href="/train/<%= train.id %>"><%= train.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>
    There are no trains, add one.
  </p>
<% end %>

<a href="/trains/new">Add Train</a>
<a href="/">Back</a>
